# ============================================================
# 필요한 파이썬 패키지 목록
# 설치: pip install -r requirements.txt
# 버전을 ">=" 로 느슨하게 잡아 파이썬 3.10~3.12 환경에서 두루 설치되게 했다.
# ============================================================

# --- 데이터 처리 ---
pandas>=2.0            # 표(DataFrame) 형태 데이터 처리
numpy>=1.24            # 수치 계산 (배열, 로그, 평균 등)

# --- 모델링 ---
scikit-learn>=1.3      # 로지스틱회귀, 랜덤포레스트, 교차검증, 평가지표
lightgbm>=4.0          # 그래디언트 부스팅 모델 (표 형식 데이터에 강함)

# --- 모델 설명(해석) ---
shap>=0.44             # SHAP 값 계산: "이 예측이 왜 이렇게 나왔는가"를 설명

# --- 시각화 ---
matplotlib>=3.7        # 기본 그래프
seaborn>=0.12          # 통계 그래프 (matplotlib 위에서 동작)

# --- 설정/환경 ---
python-dotenv>=1.0     # .env 파일에서 설정값·비밀값을 읽어오는 도구

# --- 노트북 실행 ---
jupyter>=1.0           # 주피터 노트북 실행 환경
ipykernel>=6.25        # 노트북이 파이썬 커널과 연결되도록 해주는 패키지
nbformat>=5.9          # 노트북(.ipynb) 파일을 코드로 생성/편집할 때 사용

# --- 데이터 내려받기 ---
kagglehub>=0.3          # Kaggle 데이터셋을 코드로 내려받는 공식 도구
